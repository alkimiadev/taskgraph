//! Find bottleneck tasks (high betweenness centrality).

pub fn execute() -> crate::Result<()> {
    // TODO: Find tasks with high betweenness centrality
    // These tasks are on many dependency paths
    // Output: ranked list with scores
    Ok(())
}
